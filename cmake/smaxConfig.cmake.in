@PACKAGE_INIT@

# RedisX CMake configuration file

include(CMakeFindDependencyMacro)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/smaxTargets.cmake")

# Get include directories from target properties
get_target_property(smax_INCLUDE_DIRS smax::core INTERFACE_INCLUDE_DIRECTORIES)

find_dependency(redisx)
find_dependency(xchange)
find_dependency(Threads)

if(@ENABLE_OPENMP@)
    find_dependency(OpenMP)
endif()

if(@ENABLE_TLS@)
    find_dependency(OpenSSL)
endif()

check_required_components(redisx)


